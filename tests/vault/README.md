# Test Vault

Minimal Kronos vault for GRIM debug mode.

Contains a small set of FDOs for testing graph flow without touching the real vault.
Use `GRIM_ENV=debug` or `--debug` flag to use this vault instead of the real one.
